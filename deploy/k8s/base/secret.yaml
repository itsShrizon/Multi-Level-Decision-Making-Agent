# Stub Secret. In real environments this comes from a SealedSecret /
# External Secrets / GCP Secret Manager (see overlays/prod). The base
# resource lets kustomize wire envFrom; values are placeholders.
apiVersion: v1
kind: Secret
metadata:
  name: mldm-agent-secrets
type: Opaque
stringData:
  OPENAI_API_KEY: "replace-me"
  GEMINI_API_KEY: ""
  DATABASE_URL: "postgresql+asyncpg://user:pass@cloudsql-proxy:5432/chatbot_db"
  REDIS_URL: "redis://redis-master.cache.svc.cluster.local:6379/0"
  SENTRY_DSN: ""
