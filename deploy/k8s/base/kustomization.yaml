apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/name: mldm-agent
  app.kubernetes.io/component: api

resources:
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
